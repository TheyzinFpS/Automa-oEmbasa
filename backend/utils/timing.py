# Utilitarios mantidos por compatibilidade com fluxos antigos.
# A automacao atual nao aplica delay artificial entre etapas SAP.


def paced_sleep(base_seconds: float = 0.0, multiplier: float = 1.0) -> float:
    return 0.0


def observation_pause(multiplier: float = 1.0) -> float:
    return 0.0
